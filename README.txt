【概要】
本ソフトは、YouTubeの動画やプレイリストを自分専用のHLSライブストリームに変換し、VRChat内の各種ワールドプレイヤー（iwaSync3, YPlay, ProTV等）でシームレスに再生するためのツールです。
通常のGUI操作に加え、外部アプリからHTTP JSON API経由で操作可能な「ヘッドレス（APIサーバー）モード」も搭載しています。

==================================================
🚀 【起動方法（同梱バッチ）】
==================================================
- VRCYouTubeStreamer (Normal).bat
    Cloudflareトンネルを有効にして起動します。VRChat内で再生する場合はこちらです。
    サーバーはループバック（127.0.0.1）のみで待ち受け、公開はトンネル経由に限定されます。

- VRCYouTubeStreamer (Local Test).bat
    トンネルなしのローカルテスト用。同一Wi-Fi内のスマホからも開けるよう
    全インターフェース（--host 0.0.0.0）で待ち受けます。QR共有の動作確認向けです。

- VRCYouTubeStreamer (Headless Test).bat
    GUIを出さずAPIサーバーとしてのみ起動します（トンネルなし・LANアクセス可）。

==================================================
🎮 【GUI操作と主な機能】
==================================================
1. キューのドラッグ＆ドロップ並び替え:
   各動画の「☰」マークまたはタイトルをドラッグして、直感的に順番を入れ替えられます（リストの上下端に持っていくと自動スクロールします）。従来の「▲」「▼」ボタンによる並び替えも併用可能です。

2. シャッフル再生 & キュー保持ループ再生:
   - [🔀 Shuffle List] : 現在のキューを一瞬でランダム順に並べ替えます。
   - [🔀 Shuffle] スイッチ : キューから次に再生する動画をランダムに選んで再生します。
   - [🔁 Loop] スイッチ : 再生が終了した動画をキューの末尾に自動再登録し、同じリストをエンドレスにループ再生します。

3. ⚙ Settings（設定画面）:
   画面右上の「⚙ Settings」ボタンから、以下の設定を変更・保存できます：
   - サーバーポート (デフォルト: 8000)
   - HLSセグメント/バッファリング秒数 (デフォルト: 3秒)
   - 動画切り替わり時のバッファ待機秒数 (デフォルト: 1秒)
   - Web Player 再生同期バッファ数 (デフォルト: 4)
   - 起動時の Loop / Shuffle デフォルト有効設定
   ※ 設定ダイアログ内の「▶ API 仕様・引数リファレンス」をクリックすると、外部連携用のAPI一覧を展開・確認できます。

4. 📻 BGM / ラジオモード（低帯域配信）:
   YouTube動画から音声のみを抽出し、静止画と合成して配信します。合計帯域を約250〜350kbps（通常配信比90%以上削減）に抑えられます。
   背景（radio_bg_source）は以下から選べます：
   - card      : サムネイル＋楽曲情報の自動生成カード画面（デフォルト）
   - slideshow : アップロードした写真のスライドショー（自動送り対応）
   - standby   : 待機画面（QRコード）

5. 📱 スマホ共有用 QRコード & Webリクエスト機能:
   - 待機画面（Standby）: キューが空になると、VRChat内の画面に「QRコード ＆ WebリクエストURL」が描画された待機映像がループ配信されます。
   - スマホからの動画追加: ワールド内のプレイヤーがスマホカメラでQRコードを読み取るとWebリモコン画面が開き、YouTube URLを貼り付けて「キューに追加」を押すだけで自動再生されます。
   - [📱 QR Code] ボタン : アプリ画面上のボタンからいつでもQRコードウィンドウを開いて提示・共有できます。

6. 🛡️ セキュリティ機能（多層防御）:
   - アプリ終了・設定変更・キュー全消去などの管理操作は、既定ではホストPC本人（ループバック）からのみ許可されます。
     それ以外からのアクセスは自動遮断（403 Forbidden）され、動画追加のみ安全に許可されます（SSRF・ローカルファイル保護、連投制限付き）。
   - 同一LAN内の端末（スマホ等）は「ゲスト」として扱われ、config.json の
     allow_web_queue_add / allow_web_queue_edit / allow_web_playback_control の範囲でのみ操作できます。
   - 同一LAN内をホストPCと同等に信頼したい場合のみ、config.json で "trust_lan_clients": true を明示的に指定してください。
     （公共Wi-Fiなど、信頼できないネットワークでは有効にしないでください）

==================================================
🌐 【Web リモコン（ブラウザ / スマホ）】
==================================================
ブラウザで「 http://localhost:8000/ 」（またはトンネル公開URL）にアクセスすると、Webリモコン画面が開きます。
※ ポートは config.json の "port" で変更できます。上記URLは既定値のものです。

- インライン HLS プレビュー: 配信中の映像・音声をその場で試聴できます（折りたたみ可）。
- キューのリアルタイム閲覧・追加・並び替え・削除。
- 再生制御（スキップ / ループ / シャッフル）とラジオ背景・写真表示時間の切り替え。
- QRコード表示とストリームURLのワンクリックコピー（スマホ共有用）。
- 画面幅に応じたレスポンシブ表示に対応し、スマホ縦画面でも操作できます。

この画面のHTMLは ui/index.html を正本とし、EXEに同梱されています。
UIを差し替えたい場合は、VRCYouTubeStreamer.exe と同じフォルダに ui\index.html を置くと、そちらが優先して読み込まれます。

==================================================
🤖 【ヘッドレス・APIサーバーモード (外部アプリ連携)】
==================================================
コマンドラインから以下のように起動することで、GUIを表示せずバックグラウンドのAPIサーバーとして動作します：

  VRCYouTubeStreamer.exe --headless --port 8080

【主な起動オプション】:
- --headless / -hl     : GUIを表示せずAPIサーバーとして起動
- --tunnel             : Cloudflareトンネルを有効化
- --no-tunnel / -nt    : Cloudflareトンネルを無効化（ローカルテスト）
- --port / -p <番号>   : サーバーポート（既定: 8000 または config.json）
- --host <アドレス>    : 待ち受けアドレス（既定: 127.0.0.1 または config.json）
                         LAN内のスマホから開く場合は 0.0.0.0 を指定します。

【主なAPIエンドポイント (JSON / CORS対応)】:
- GET  /api/status   : ストリーマーの状態、URL、再生中動画、キュー一覧の取得
- POST /api/queue    : キューへの動画URL追加 ({"url": "https://..."})
- POST /api/control  : 再生制御 ({"action": "skip" / "clear_queue" / "stop" / "shuffle" / "set_loop" / "set_shuffle"})
- POST /api/shutdown : サーバーおよびプロセスの安全終了
- GET  /api/config   : 現在の設定値の取得
- POST /api/config   : 設定値の更新・保存
- GET  /api/qrcode   : 共有用QRコード画像（PNG）

==================================================
⚠️ 【重要：VRChat内での設定について】
==================================================
VRChat内で本ツールの配信（*.trycloudflare.com）を読み込ませるには、VRChatのメニューから以下の設定を有効にする必要があります。この設定がオフの場合、動画が再生されません。

設定箇所: Settings ＞ Downloads & Share
設定項目: Allow Untrusted URLs（信頼できないURLの許可）をオンにする

💻 【動作環境・必要なもの】
対応OS: Windows 10 / 11 (64bit)
ネットワーク: 安定したインターネット接続環境（アップロード帯域を使用します）
VRChatプレイヤー: iwaSync3, Yacc, KineLister, ProTVなど、HLSストリーム（.m3u8）に対応したワールドプレイヤー

ℹ️ 【製品の仕様・制限事項】
- URLの再生成について: Cloudflareの一時トンネル技術を使用しているため、本ツールを再起動するたびにVRChatに入力するストリームURLが変わります。起動中はずっと同じURLでご利用いただけます。
- YouTubeの仕様変更について: YouTube側の仕様変更により、動画の取得やプレイリストの展開が一時的にできなくなる場合があります。

⚖️ 【ライセンス表記（FFmpegについて）】
本パッケージには、動画変換処理を行う外部ツールとして FFmpeg（GPL/LGPLライセンス）の実行ファイル（ffmpeg.exe）が同梱されています。
本アプリはFFmpegのバイナリを改変せず外部プロセスとして呼び出しています。
FFmpegのソースコードは、公式サイト（ https://ffmpeg.org ）より入手可能です。

🚫 【免責事項】
本ツールは個人利用およびテスト目的のツールです。本ツールの使用によって生じたあらゆる損害、損失、またはトラブルについて、製作者は一切の責任を負いません。自己責任においてご利用ください。
