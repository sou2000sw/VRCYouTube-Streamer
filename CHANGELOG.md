# 更新履歴 (CHANGELOG)

## [2.9.4] - 2026-09-03

### 🎨 ホスト画面をモダンUIへ刷新（Webリモコンと同じ画面になった）
- **画面を二重に実装していた**: ホストの CustomTkinter 画面と Webリモコン
  (`ui/index.html`) が別実装だったため、デザインも機能網羅も食い違っていた。
  設定はホスト8セクションに対しWeb5項目、配信プレビューはWebのみ、
  ドラッグ&ドロップ並び替えの挙動も別物、という状態だった。
- **正本を `ui/index.html` 一つに寄せた**: ホスト画面は同じHTMLを WebView2
  (`pywebview`) で開くだけにした。サイドバーナビ・配信プレビュー・キュー操作・
  QR共有まで、スマホのWebリモコンとホストPCで完全に同じ画面になる。
- **ブラウザにできないことだけネイティブに残した** (`host_window.py`):
  - 🎬/🖼 **PCから直接追加（容量上限なし）**: ファイルをコピーせず元の場所を参照する。
    Webアップロード経路（200MB上限）とは別物なので、外付けHDD上の長尺MP4も追加できる。
  - 待機画像の選択、アプリの終了、ストリームキー入りURLの表示（既定はマスクのまま）。
- **ホスト専用の設定をWeb画面へ追加**: サーバーポート / Cloudflareトンネル /
  HLSセグメント秒数 / 動画切替待機 / 同期バッファ数 / 待機画面 /
  ループ・シャッフルの既定 / Webリモコンの権限3種 / パスワード。
  ホスト(localhost)からのみ表示・保存でき、ゲストのブラウザには出さない
  （出すと「押せるのに403」になるだけなので、UIごと隠す）。
- **ストリームキーは既定でマスク**: ワールド用URLは伏せ字で表示し、👁ボタンを押した時だけ
  平文にする（コピーは常に本物）。従来のデスクトップ画面と同じ扱い。
  画面共有・配信に映り込むと配信を乗っ取られるため、ここを素通しにすると
  **画面を置き換えただけでセキュリティが落ちる**。実装途中で一度その状態を作ってしまい、
  ビルド後のEXEで平文表示されているのを見つけて塞いだ（`test_host_window.py` で固定）。
  鍵を持たない HLS の `.m3u8` URL はマスクしない（読めないだけで害になるため）。
- **壊れたら従来画面へ落ちる**: WebView2 ランタイムが無い環境では
  CustomTkinter 画面が開く。`--classic-ui` / `config.json` の `host_ui: "classic"`
  でも明示的に戻せる。「起動したのに何も出ない」が最悪なので fail-safe にした。
- **pywebview の落とし穴**: `js_api` に渡すオブジェクトの**公開属性は再帰的に走査される**。
  `self.window` のように持つと .NET オブジェクトを辿って
  `maximum recursion depth exceeded` を吐き、`StreamerCore` のメソッドまでページへ露出する。
  内部参照はすべて `_` 始まりにしてある（`test_host_window.py` で固定）。

### 🎨 アプリの意匠を自前のものに統一（YouTubeロゴの使用をやめた）
- **他社サービスのロゴをアプリの顔にしていた**: ヘッダーのブランドマークが YouTube の
  ロゴだった。本ツールは YouTube 専用でもない（X / Instagram 等も通る）ため、
  意匠としても実態と合っていなかった。自前のアイコンへ差し替えた。
- **EXE / タスクバー / Alt+Tab のアイコン**: `assets/app_icon.ico` を同梱してビルド時に適用。
  元画像は1024pxで下部に文字が入るため、そのまま縮小すると16pxで潰れる。
  **マーク部分だけを正方形に切り出し**、7サイズ(16〜256px)を1ファイルに収めてある。
- **用途で2枚に分けた**: favicon 用は背景付きのタイル(`app_icon.png`)、
  ヘッダー用は背景なしの白抜き(`app_mark.png`)。ヘッダーに背景付きを置くと
  カードの色と溶けて「小さな四角」にしか見えないため。
- URL入力欄のアイコンも YouTube から中立なリンクの意匠へ変更（この欄は YouTube 専用ではない）。
- 従来画面(CustomTkinter)にもウィンドウアイコンを設定。ソースから起動した時に
  Python の既定アイコンのままだったため。

### 🎬 配信プレビューを既定で閉じた状態に変更（Webリモコン / ホスト画面 共通）
- 開いたまま起動すると、画面を見ていなくても HLS セグメントを取得し続け、帯域とCPUを消費していた。
  ホスト画面は起動しっぱなしで使うため影響が大きい。既定を「閉」にし、
  ヘッダーの「プレビュー表示」を押した時だけ読み込みを始めるようにした。
  起動直後の `.m3u8` / `.ts` 取得は0件になる。

### 🌐 Webリモコンの外部CDN依存を排除（オフラインでもUIが崩れない）
- **UIの土台を3本ともCDNから読んでいた**: `ui/index.html` は Tailwind CSS /
  Remix Icon / hls.js を CDN 直リンクで読み込んでいた。ホストPCがオフラインの時、
  あるいは CDN が塞がれている回線（社内・学校・一部モバイル）では、
  **配色もレイアウトもアイコンも無い「素のHTML」**が出るだけで操作できなかった。
  ローカル配信ツールなのに第三者サーバーが落ちたら使えない、という依存でもあった。
- **同梱配信に切り替えた**: `ui/vendor/` に Tailwind 3.4.16 / Remix Icon 4.2.0
  (woff2+woff) / hls.js 1.5.17 を置き、`/vendor/<file>` から配信する。
  バージョンは固定（`@latest` は、ある朝いきなり壊れる）。
- **CDN はフォールバックとして残した**: `vendor/` が欠けている場合だけ、
  従来どおり CDN から取り直す。UI だけ差し替えた利用者を切り捨てないため。
- **配信の作法**: 許可拡張子(.js/.css/.woff2/.woff)のみ・ディレクトリ遡上は拒否。
  `ui/` 配下を丸ごと露出させない。テキストは gzip 圧縮して返し（1.4MB → 約284KB）、
  `?v=<version>` 付きで7日キャッシュさせる。トンネル経由のゲストが毎回引かないようにする。
  ログイン画面自体がこれらで描画されるため、認証の外に置いてある。
- **ビルドで検証する**: リリース検証に「同梱アセットがEXEから配信できるか」を追加。
  オンラインでは CDN フォールバックで動いてしまい、**オフラインの利用者環境でだけ**
  UI が崩れるため、こちらから再現できない。v2.6.0 の「UIがEXEに入っていなかった」
  と同種の事故を、同じ場所で止める。
- VRCBeacon プラグインZIPにも `plugin/ui/vendor/` を同期する（`build_exe.py`）。
- `test_ui_assets.py` を追加（12件）: 参照ファイルの実在、CDN直リンクへの逆戻り検知、
  同梱していないフォント形式の参照、遡上・拡張子の拒否、gzip、HTTP配信、`?v=` の置換。

### 変更ファイル
- `assets/app_icon.ico` / `assets/app_icon.png` / `assets/app_mark.png`（新規: アプリの意匠）
- `host_window.py`（新規: WebViewホスト画面とネイティブブリッジ）
- `gui_streamer.py`（モダンUIの起動経路と `--classic-ui` フォールバック）
- `streamer_core.py` / `config.dist.json`（`host_ui` 設定を追加）
- `test_host_window.py`（新規: js_api の公開範囲・フォールバック・ホストUI要素）
- `ui/index.html`（ホスト専用UI/設定の追加、CDN直リンク → `./vendor/` 参照 + フォールバック）
- `ui/vendor/`（新規: tailwind.min.js / remixicon.css / remixicon.woff2 / remixicon.woff / hls.min.js）
- `api_server.py`（`/vendor/` 配信、gzip・キャッシュ、`__APP_VERSION__` 置換）
- `build_exe.py`（プラグインへの vendor 同期、リリース検証に同梱チェック追加）
- `THIRD_PARTY_LICENSES.txt`（Tailwind CSS / Remix Icon / hls.js を追記）
- `test_ui_assets.py`（新規）

## [2.9.3] - 2026-09-02

### 🐛 アップロードした動画だけ画質が落ちる問題を修正
- **ローカルファイルを無条件に再エンコードしていた**: `play_video` の分岐が
  `if has_qr or has_clock or is_local:` で、オーバーレイが無くても
  ローカルファイルというだけで再エンコード経路へ入っていた。URL追加(YouTube等)は
  同じ条件で `-c:v copy` に落ちて**無劣化のまま**届くため、同じ映像でも
  「Webからアップロードした瞬間に汚くなる」状態だった。
  実測(1080p30 / 原本4.76Mbps・カット4回とパンを含む素材): HLS出力時にVRChatへ届く映像の
  原本比 **SSIM 1.000000 → 0.991780**（2500kbps へ再圧縮されるため）。
- **安全に通せるものは通す**: 再生前に一度だけプローブし、
  H.264 / yuv420p系 / `max_video_height` 以内 / CFR のすべてを満たす場合のみ
  `-c:v copy` で送出する。判定できない項目が一つでもあれば再エンコードへ倒す(fail-closed)。
  HEVC・VP9・10bit・4:2:2 は AVPro で再生できない環境があるため通さない。
  VFR を通すとタイムスタンプがそのまま下流へ渡り音ズレの原因になるため通さない。
- **プローブはファイルごとに1回だけ**: 結果をアイテムにキャッシュする。
  低速な外付けHDD上のファイルで再生のたびに走らせない。呼び出しは v2.9.1 の
  `_run_probe()` 経由なのでハングしない。

### 計測して**見送った**変更
- 送出側の `ultrafast`→`veryfast` / `baseline`→`high`（ビットレート据え置き）:
  SSIM 0.991780 → 0.991889 で**誤差**。効いていたのは preset や profile ではなく
  2500kbps という絞り込みそのものだった。
- RTMP(Topaz)出力時に中間エンコードを可逆寄りにする案: 無劣化基準0.993073 に対し
  現状0.992238 / 提案0.992404。シンクの 720p・1500kbps が支配的で中間の傷は埋もれる。
  効果が無いため入れていない。

### 🔍 障害解析用のログを追加
- **`--noconsole` ビルドではログがどこにも残らなかった**: `sys.stdout` がダミーに
  差し替わるため、配布先で起きた事象を後から追う手段が無かった。
  `log_print` がアプリと同じフォルダの `vrc_media_streamer.log` へも書くようにした。
  2MBを超えたら `.log.1` へ1世代だけ退避する。書き込み失敗は握り潰す。
- 送出経路（radio 200k / image 1500k / video 2500k / copy）と、
  取り込んだ画像の元解像度・形式・バイト数をログに残すようにした。
  「届いた素材が既に小さいのか」「どの経路で出したのか」を切り分けられなかったため。
- `conftest.py`: テストがこの実ログへ追記してしまうため、出力先を一時ファイルへ隔離する
  autouse フィクスチャを追加（pytest 1回で289行が実ログへ流れ込んでいた）。

## [2.9.2] - 2026-09-01

### 🐛 写真を大量に一括追加すると配信が停止する問題を修正
- **1枚追加するたびにストリームを再構築していた**: `add_image_file` 等が写真1枚ごとに
  `reload_stream_event` を立てるため、監視ループが受けるたびに ffmpeg を
  kill → 0.2秒待ち → 再起動していた。GUIから70枚まとめて追加すると、
  **約22秒間ずっと300msごとにエンコーダが再起動され続け**、HLSセグメントが安定して
  出力されず配信が止まる。実測: 2048x1440のスクショで最適化処理 309ms/枚・出力3.61MB/枚、
  70枚で21.6秒・253MB。
- **再構築要求をデバウンス**: 発火側ではなく消費側（監視ループ）に入れたため、
  GUI追加・Webアップロード・API経由のすべてに効く。
  `request_stream_reload()` で要求を記録し、`_reload_due()` が
  **最後の要求から1秒静穏**になって初めて実行する。要求が途切れない場合でも
  **10秒の先送り上限**で必ず反映する（無限に延期されないため）。
  検証: 70枚バースト(21.7秒)で再構築 **70回 → 3回**。単発の設定変更は1.1秒で反映。

### 既知の課題
- 写真キャッシュがPNGのため1枚あたり約3.6MBを消費する（70枚で約253MB）。
  JPEG化すれば1/10以下になるが、concatマニフェストとオーバーレイ経路への影響が
  未評価のため今回は据え置き。

## [2.9.1] - 2026-09-01

### 🐛 外付けHDD/USB上の長尺MP4を取り込むとフリーズしうる問題を修正
- **尺取得(probe)がタイムアウト後に永久ブロックしうる**: `subprocess.run(timeout=5)` は
  TimeoutExpired を投げる前に `kill()` してから**タイムアウト無しの** `communicate()` を呼ぶ。
  子プロセスが低速な外付けHDD/USB/NASのI/O待ちで停止していると `TerminateProcess` は
  即座に効かず、この2回目の `communicate()` が戻らない。呼び出し元スレッドがそこで刺さる。
  reap 側にもタイムアウトを置き、kill が効かない場合は子を捨てて必ず戻る `_run_probe()` を新設。
- **タイムアウト5秒が短すぎた**: スピンダウンした外付けHDDは**起動だけで5〜15秒**かかる。
  ffprobe を 20秒 / ffmpeg フォールバックを 30秒 に延長。実測では ffprobe 約0.3秒に対し
  `ffmpeg -i` フォールバックはローカルSSDでも約3.1秒かかっており、遅いメディアでは
  従来の5秒を容易に超えていた。尺が取れなかった場合はログに残す。
- **🐛 配布物に `ffprobe.exe` が同梱されていなかった**: `ffmpeg.exe` のみを配布していたため、
  PATH に ffmpeg を持たない環境では必ず11倍遅い `ffmpeg -i` フォールバックに落ちていた。
  `build_exe.py` を修正し、dist/ ・ releases/ ・ plugin/bin/ の3箇所すべてで ffmpeg と対で配布する。
- **🐛 `--noconsole` ビルドで stdin ハンドルが無効**: ffmpeg は対話コマンド用に stdin を読むため、
  ウィンドウモードのPyInstallerビルドでは無効ハンドルの継承が固まる原因になりうる。
  ffmpeg / cloudflared を起動する5箇所に `stdin=subprocess.DEVNULL` を追加。

## [2.9.0] - 2026-08-28 (開発中)

### 🛠️ CLI 引数・環境設定オーバーライド機構の総点検・堅牢化 (Task 19)
- **設定が3層になった**: 優先順位は **CLI引数 / 環境変数 > `config.json` > 既定値**。
  `LayeredConfig`（`dict` のサブクラス）で層を分け、`self.config` の中身は常に実効値。
  参照212箇所・書き込み26箇所は無修正で動く。
- **🐛 CLI の一時指定が config.json に焼き付く問題を修正**:
  設定画面はフォーム全体を送り返すため、ウィジェットに入っている**CLI上書き後の実効値**が
  「利用者が変更した値」と誤認され、無関係な項目を保存しただけで永続化されていた。
  `Local Test.bat`（`--no-tunnel`）で一度起動して設定を保存すると
  `enable_tunnel: false` が恒久設定になる。保存直前に元の値へ戻す方式をやめ、
  上書きを保存対象から**構造的に**外したうえで、入口にエコー判定を追加して解消。
  明示的に別の値へ変更した場合は、これまでどおり永続化しその起動でも反映される。
- **オーバーライドできる項目を拡張**:
  `--config` / `--output-mode` / `--resolution WxH` / `--bitrate` / `--fps` /
  `--radio` / `--no-radio` に加え、**任意のキーを指定できる `--set KEY=VALUE`**（複数可）と
  環境変数 **`VRCMS_*`** に対応。値は `DEFAULT_CONFIG` の型へ変換し、未知のキーは受け付けない。
- **秘匿値の扱い**: ストリームキー・パスワードは `--set` では拒否し、環境変数のみ受け付ける
  （コマンドライン引数はプロセス一覧から第三者に見えるため）。起動ログでも値をマスク。
- **🐛 単体テストが利用者の実 `config.json` を書き換えていた問題を修正**:
  `CONFIG_FILE` がモジュール定数の絶対パスだったため、テスト実行のたびに
  `standby_image_path` へ pytest の一時パスが焼き付いていた。`config_path` を
  差し替え可能にし、`conftest.py` の autouse フィクスチャで全テストを隔離。
- 単体テスト `test_config_overrides.py` を新設（21項目）。全90件通過。


### 🩹 TopazChat配信で「音は出るが映像が乱れる」問題の修正
- **累積PTSオフセットが無視されていた**:
  `-output_ts_offset` は出力オプションだが `-i` より前に置かれており、実測で完全に無効だった
  （10秒指定しても出力PTSは素通し）。再生アイテムが切り替わるたびPTSが0付近へ巻き戻り、
  受信側で「DTS out of order」「Packet corrupt」を誘発。出力側へ移動して解消。
- **`-tune zerolatency` の除去**:
  同一1500kbpsで PSNR 36.61 → 37.55dB（SSIM 0.9751 → 0.9805）。帯域を2000kへ増やすのと
  同等の改善を、増やさず・エンコード速度も落とさずに得られることを実測で確認。
- **RTMP配信を実時間ペースに変更**:
  HLS用の15秒先読みをRTMPにも適用しており、経過4秒で約14秒ぶんを送出していた（実時間の3.5倍）。
  ライブ投稿としては不正で、低遅延目的のTopazChat利用と矛盾していた。RTMP系は1秒先読みへ。
- 実機確認: TopazChat往復で 30.1fps / デコードエラー0、end-to-end遅延 約2〜3秒。

### 📡 配信先（HLS / TopazChat / 汎用RTMP）の選択制対応 (Selectable Streaming Destination)
- **シンク（出口）の抽象化**:
  - 配信先を決めていた `ensure_hls_receiver()` を **`ensure_stream_sink()`** へ改称し、
    `output_mode`（`hls` / `topaz` / `generic_rtmp`）で出力先だけを差し替える構造に変更。
    送信側パイプライン（各再生アイテムのFFmpeg）には一切変更なし。旧名は互換のため別名として存置。
  - 既定値は従来どおり **`hls`**。配布物が黙って他者の無償サーバーへ帯域を向けないよう、
    TopazChat は明示的に選択したときだけ使われる。
- **TopazChat 対応（低遅延 約1〜2秒）**:
  - RTMP投稿先・RTSP再生URLは `config.json` の設定値（`topaz_rtmp_base` / `topaz_rtsp_base`）とし、
    ハードコードしない（個人運営のためホスト変更・終了に追随できるように）。
  - RTMP経路では **シンク側で再エンコード必須**（解像度・fps・GOP固定 → `libx264` / `aac`）。
    `-c copy` の素通しは、上限超過による強制切断とアイテム切替時のパラメータ変化で成立しない。
  - **映像2Mbps / 音声320kbps のハードリミット**を実装し、超過する設定値は保存時にクランプ。
  - UIに「本ソフトはTopazChatの公式ツールではありません」「法人利用は別途問い合わせが必要」
    「上限超過で強制切断」の注記と、公式サイト・GitHubへのテキストリンクを表示。
- **フォールバック設計 (fail-safe)**:
  - 投稿先へのTCP到達性を起動前に確認し、失敗が規定回数に達したら **自動でローカルHLSへ退避**して
    配信自体は途切れさせない。復帰試行は**指数バックオフ**（相手サーバーを叩き続けない）。
  - シンクの突然死を監視するウォッチドッグを追加（RTMPで新たに発生する障害モードへの対応）。
- **ストリームキーの機密扱い・自動生成**:
  - 32文字以上のランダムキーを自動生成（短いキーは第三者に配信を乗っ取られるため）。
  - `/api/status`・`/api/config`・POSTボディのログの全経路でマスク。生キーの取得は
    localhost 限定の `POST /api/destination`（`action: "reveal_key"`）のみ。
- **URL概念の分離**:
  - `get_status_data()` に「ワールドの動画プレイヤーに貼るURL」(`video_url`) と
    「Webリモコンを開くURL」(`remote_url`) を別フィールドとして追加。QRが焼くのは後者のみ（従来どおり）。
- **UI / GUI**:
  - Webリモコンの「配信・QR設定」タブに配信先カードを追加（モード選択・キー入力/生成・
    ビットレート・現在の実配信先バッジ・退避中の警告と即時復帰ボタン・動画URLのコピー）。
  - ホストGUI（`gui_streamer.py`）の設定画面にも同等の項目を追加。
- **単体テスト**:
  - `test_destination.py` を新設（15項目）。コマンド組み立て・上限クランプ・キーマスク・
    URL分離・フォールバック判定・到達不可時にFFmpegを起動しないこと等を検証。


## [2.7.0] - 2026-08-27

### ⏰ 配信実時刻（LIVE時計）オーバーレイ機能の修正・堅牢化 (Live Clock Overlay Fix)
- **設定フラグ（`overlay_clock_enabled` / `overlay_clock_video`）の厳格チェック**:
  - `play_radio`（ラジオ）、`play_image`（静止画/スライドショー）、`play_standby_loop`（待機画面）において、設定フラグが無効な場合は FFmpeg に一切の `-vf`（`drawtext`）を付与せずバイパスするよう修正。
  - 時計無効時の不要なエンコード負荷およびフォント不備による FFmpeg クラッシュを完全に排除。
- **位置設定共通ヘルパーの導入 (`get_clock_filter_for_config`)**:
  - `overlay_clock_position`（`top-right`, `top-left`, `bottom-right`, `bottom-left`）から最適な描画座標を計算し、`drawtext` フィルタ文字列を生成する処理を一元化。
- **フォント探索とエスケープの強化 (`get_drawtext_font_path`)**:
  - Windows・Linux・macOSのフォント探索候補を拡充し、パス区切り文字およびコロンのエスケープ（`C\:/...`）を安全に処理。
- **包括的な単体テスト整備**:
  - `test_clock_features.py` に時計フィルタヘルパー、位置座標計算、`-filter_complex` ビルダー組み合わせ、および各配信パイプラインでのフラグチェック検証（計8項目）を追加。

### 📱 待機画面・通常動画・ラジオ全画面でのQRコード表示モード統括・修正 (QR Code Overlay & Standby Modes Fix)
- **待機画面固定画像モード (`standby_mode: "image"`) での QR 合成対応**:
  - `standby_mode == "image"` 時に `overlay_qr_enabled: true` が設定されている場合、固定画像（デフォルト画像・カスタム画像・フォールバック画面）上に Pillow (`alpha_composite`) でQRコードカードを自動合成するよう修正。
- **動画・ラジオ共通の FFmpeg フィルタグラフビルダー (`_build_video_filter_complex`)**:
  - 音声ストリームの分離有無に応じた入力インデックス（`qr_idx`）や `scale2ref` / `overlay` / `drawtext`（時計）の組み合わせを堅牢に組み立てるビルダーメソッドを新設。
- **ラジオモードの QR オーバーレイ対応 (`play_radio`)**:
  - `card` / `standby` 背景使用時、`overlay_qr_enabled` が有効な場合は動画再生時と同様に FFmpeg `-filter_complex` でQRコードを合成して配信。
  - スライドショー（`slideshow`）は `get_image_for_playback()` でPillow合成済みのため、二重合成を自動防止。
- **ユニットテストの整備**:
  - `test_standby.py`（待機画面QR合成検証2件）および `test_qr_overlay.py`（ビルダー・QR画像生成検証9件）を追加。

### 🔒 Webリモコン パスワード/PIN認証保護機能 (Web Remote Password Protection / Web PIN)
- **PIN / パスワード認証によるリモコン保護**:
  - `config.json` に `"web_password": ""`（未設定時は認証なし＝オプトイン）を追加。
  - GUI（`gui_streamer.py`）の設定画面に「Webリモコン パスワード/PIN (空欄で無効)」入力欄および表示/非表示切替を追加。
  - ホスト本人（localhost / ループバック）からのアクセスは常に認証不要（自動バイパス）。
- **ゼロ負荷ロック & ポーリング完全停止**:
  - パスワードが設定されている場合、ゲスト（スマホ/外部ブラウザ）がアクセスするとPIN/パスワード入力モーダルを表示して画面をロック。
  - **ロック中は `/api/status` の定期ポーリングを完全停止**し、不特定多数からのアクセスや放置タブによるホストPCのCPU/ネットワーク負荷をゼロに抑制。
- **セッション管理 & ログアウト機能**:
  - 認証成功時はブラウザのセッション（`sessionStorage`）に記憶し、タブを閉じると自動破棄（長期間残り続けない安全設計）。
  - UIヘッダーに「ロック / ログアウト」ボタンを追加し、いつでも再ロック可能。
  - URLパラメータ（`?pin=xxxx` / `?password=xxxx`）による初期認証補完に対応。
- **APIセキュリティ強化**:
  - ゲストからのAPIリクエスト（`/api/status`, `/api/queue`, `/api/control`, `/api/upload`, `/api/qrcode`）で `X-Web-Password` ヘッダーを検証し、未認証・不一致時は即座に `401 Unauthorized` を返却。
  - パスワード検証エンドポイント `POST /api/auth` を新設。
  - ※VRChat内の動画プレイヤー通信（`/stream.m3u8`, `*.ts`）は認証対象外とし、ワールド内再生への影響なく稼働を維持。

### 📺 ライブプレビュー再生・自動復帰の堅牢化 (Live Preview Player Stabilization)
- **ブラウザ自動再生制限（Autoplay Policy）の解消**:
  - Chrome / Edge / Safari 等でDOM上に `muted` 属性がない動画要素の自動再生がブロックされる問題を解消するため、`<video id="liveVideo" muted>` 属性を付与（音量バーでいつでもアンミュート可能）。
- **ステータスポーリングによる Hls.js インスタンスの連続破壊を防止**:
  - `updateUiOnline` からの安易な `initHlsPlayer()` 再呼び出しを撤廃し、初回1回のみ初期化・以後は Hls.js 内部のエラーバックオフタイマーに任せる堅牢な設計に改善。
- **HLSリトライループ脱落バグの修正**:
  - ページ読み込み時やサーバー起動前のオフライン状態でHLSマニフェスト取得に失敗した際、エラータイマー内でリトライが早期終了して停止していた問題を修正。プレビュー有効中は安全なバックオフ付きで再試行を継続するよう改善。
- **HLS MIMEタイプ & リクエストルーティング適正化**:
  - Windows環境で `.ts` が `text/plain` になるのを防止するため、`.m3u8` (`application/vnd.apple.mpegurl`) と `.ts` (`video/mp2t`) のMIMEタイプを明示。
  - Safariやネイティブプレイヤーでの `/stream.m3u8` リクエスト時にHTMLが誤返却されるリスクを排除。

### 🧹 終了時クリーンアップの完全化 (Complete Shutdown Cleanup)
- **FFmpeg子プロセスの終了待機によるファイルロック解放**:
  - `kill_proc(proc)` で `proc.kill()` の後に `proc.wait(timeout=2.0)` を呼び出し、Windowsの排他ファイルハンドルが確実に解放されるのを待機してから削除を実行。
- **終了時の一時ファイル・ディレクトリ完全消去**:
  - アプリ終了時に `hls_output/` 配下の全セグメント（`.ts`）・マニフェスト（`.m3u8`）・一時画像（`.png`）・サブフォルダを完全に消去（0件）するよう強化。
- **GUI終了フローの正常化**:
  - `gui_streamer.py` の `on_closing` で `os._exit` による atexit スキップを廃止し、`sys.exit` で確実にクリーンアップハンドラを通すよう修正。

### 🖼️ スライドショー再生時 URL/QR表示フリーズの修正 (Slideshow QR Overlay Fix)
- **画像の 1920x1080 自動正規化 (`get_image_for_playback`)**:
  - 元写真の解像度やアスペクト比に関わらず、必ず 1920x1080 キャンバス（アスペクト比維持レターボックス）に正規化してから QR/URL オーバーレイを合成するよう刷新。Pillow のアルファブレンド座標範囲外エラーや FFmpeg `concat` デマルチプレクサの解像度不一致によるデコードハングを完全解消。
- **一時ファイル名の一意化（ハッシュ＋インデックス）**:
  - スライドショー生成時に同一秒タイムスタンプでファイルが上書き破壊される問題を解消。
- **設定変更ホットリロード時のプロセス完全終了待機**:
  - 写真再生中のQR設定変更時、前の送信プロセス終了を確実に待機してから新プロセスを立ち上げるように改善。

### 🎬 実機検証済み対応動画元・プラットフォームの仕様明記
- **動作確認済みプラットフォームの整理**:
  - YouTube動画、YouTube Shorts（ショート）、YouTube Live（ライブ配信）、X (旧 Twitter) 動画、Instagram（投稿・リール動画）での実機動作確認を完了。
  - Twitch（一部配信のみ対応、仕様により非対応チャンネルあり）、ニコニコ動画（非対応）の動作状況をドキュメントおよびWeb UIに明記。

## [2.7.0] - 2026-08-27

### 📻 再生モード3分岐 ＆ 写真・動画キュー完全分離 ＆ スライドショー安定化 (Playback Modes & Photo Pool Separation)
- **3大再生モード（`playback_mode`）への明確な分岐**:
  - 🎬 **動画モード (`video`)**: 動画キューを通常再生。写真は一切混入しません。
  - 📻 **ラジオモード (`radio`)**: 動画音声のみ抽出し、背景（アルバムアートカード / 写真プールスライドショー / 待機画面）と合成して極小帯域（~300kbps）で配信。
  - 🖼️ **スライドショーモード (`slideshow`)**: 写真プール（アルバム）内の写真を指定秒数ごとに自動巡回配信（無音）。
- **動画キュー (`play_queue`) と 写真プール (`photo_pool`) の完全分離**:
  - アップロードされた写真は独立した写真プールに保持され、動画を再生しても消滅しません。
  - 写真プール管理（個別削除、並び替え、全消去、上限200枚）を実装。
- **写真0枚時のフォールバック案内バー合成（パターンA）**:
  - 写真が0枚の状態でスライドショーが選択された場合、待機画面（固定画像・QR案内）の下部に半透明案内バー（`📷 スライドショー写真が未登録です（Webリモコンから写真をアップロードできます）`）を Pillow で合成して配信。写真がアップロードされ次第即座にスライドショーへ復帰。
- **セッション限定キャッシュのクリーンアップ**:
  - 起動時・終了時に `hls_output/images/` 配下の全キャッシュ画像を完全削除。PC内に一時写真が残留しない安全設計。
- **Web UI & ホストGUI の刷新**:
  - Webリモコンに 3ボタントグル ＆ 写真プール（共有アルバム）管理カードを追加。
  - ホストGUI（デスクトップ）に 3モードセグメントボタン（`🎬 Video` / `📻 Radio` / `🖼️ Slideshow`）を配置し、メイン画面を `CTkTabview`（`🎬 Video Queue` / `📷 Photo Pool`）によるタブ構成にアップグレード。

## [2.6.0] - 2026-08-25

### 🧩 Webリモコン ＆ VRCBeacon プラグインUIの完全統合 ＆ UI正本配置の整理 (Integrated Web Remote & Plugin UI)
- **WebリモコンとプラグインUIの一本化**:
  - 従来別々に存在していた `api_server.py` のWebリモコンHTMLとプラグインUIを1つの洗練されたモダンUI（Tailwind CSS + Remix Icon）に統合。
  - ルート直下に `ui/index.html` を正本として配置し、VRC_Media_Streamer本体のUI資産として責務を明確化。
  - `build_exe.py` のパッケージング処理により、プラグイン配布フォルダ `plugin/ui/index.html` への自動同期を実現。
  - `api_server.py` の `GET /` 配信で `ui/index.html` を配信するよう一本化。
- **インライン HLS ライブストリームプレイヤー (`hls.js`) の統合**:
  - Webブラウザやスマートフォンから配信中の動画・音声・スライドショーをその場で試聴・プレビューできるプレイヤーを装備（折りたたみトグル対応）。
- **スマホ共有用 QRコード表示 & ストリームURLコピー**:
  - 「接続 & スマホ共有」タブおよびプレビュー画面に `/api/qrcode` 画像とワンクリックURLコピーを設置。
- **権限連動 & レスポンシブ対応**:
  - ホスト（Beacon内/ローカル）とWebリモコン（リモート）の権限（`permissions`）に応じて操作ボタンの活性/非活性を自動制御。
  - スマホ縦画面でも快適に操作できるよう、画面幅に応じたレスポンシブナビゲーションを実装。
- **Beacon Bridge と通常ブラウザ（ローカル/トンネル）の両立**:
  - VRCBeacon 内では `postMessage` 経由でホスト特権通信を行い、ブラウザで開かれた際は自動で直 `fetch` 通信にフォールバック。

### 🛠️ 配布パッケージの修正（リリースレビュー反映）
- **`ui/index.html` が配布物に含まれていなかった問題を修正**:
  - PyInstaller に `--add-data "ui;ui"` を追加し、UI 正本を EXE へ同梱。
  - `get_ui_html()` の探索順を「EXE と同じフォルダ → スクリプト位置 → カレント → `sys._MEIPASS`」に整理し、利用者による UI 差し替えを可能にしつつ、同梱版を確実なフォールバックとした。
- **内蔵フォールバックテンプレートの破損を修正**:
  - `HTML_PLAYER_TEMPLATE` は `<body>` と DOM マークアップおよび `<script>` 開始タグを欠いており、配信されると JavaScript がテキストとして表示される状態だった。
  - UI を2箇所に複製する構造そのものが原因のため内蔵複製を廃止し、正本が見つからない場合は原因と復旧手順を示す診断ページを返す fail-closed 方式に変更。
- **プラグイン ZIP に開発時のアップロード写真が同梱されていた問題を修正**:
  - パッケージング時に `hls_output/`・`__pycache__/`・`*.log` 等の実行時生成物を除外し、`plugin/bin/hls_output/` を明示的に削除するようにした。
- **`config.json` の `host` 設定が無視されていた問題を修正**:
  - トンネル無効時に `host` 指定を無視して全インターフェースへバインドしており、`"127.0.0.1"` 指定でも実際は `0.0.0.0` で待ち受け、ログ表示も実態と食い違っていた。設定値をそのまま尊重し、実際のバインド先をログに出力するよう修正。
- **同一 LAN の端末がホスト権限を持っていた問題を修正**:
  - トンネル無効時に同一 LAN の任意の端末が `/api/shutdown`・`stop`・`clear_queue` を実行できた。管理操作は既定でループバック限定とし、LAN 端末は `allow_web_*` の範囲で操作する「ゲスト」として扱う。従来動作は `"trust_lan_clients": true` で明示的にオプトイン可能。
- **配布用 config の分離**:
  - 開発中の作業用 `config.json`（ポート 8997・トンネル無効等）がそのまま配布されていたため、`config.dist.json` を配布用テンプレートの正本として追加。
- **CORS ヘッダの二重送出を修正**:
  - `end_headers()` と個別ハンドラの双方が付与しており、`Access-Control-Allow-Origin` が2つ送出されていた（仕様上クロスオリジン `fetch` が失敗する）。
- **ドキュメントの整合**:
  - 起動バッチのポート表記（8000）と実設定の不一致を解消し、`README.txt` を `.gitignore` 対象の `dist/` からリポジトリ直下へ移動して追跡対象にした。
- **コマンドライン引数が `config.json` に焼き付いていた問題を修正**:
  - `--port` / `--host` / `--no-tunnel` の指定が `self.config` に直接載っていたため、UI から何か設定を変更して `save_config()` が走るたびに、その起動限りのはずの値が `config.json` に永続化されていた。これが「開発者のローカルテスト設定がそのまま配布物の既定値になる」根本原因だった。
  - CLI 由来の上書きは保存時に元の値へ戻すようにし、利用者が UI から明示的に変更した項目のみ永続化する。
- **ビルド後検証で起動した EXE が終了しきらない問題を修正**:
  - `proc.terminate()` では子プロセスの `ffmpeg` が生き残り、`hls_output/` を掴んだまま残留していた。`/api/shutdown` による正規終了を先に行い、実行時生成物の削除をリトライするようにした。
- **再発防止のスモークテストを追加**:
  - `build_exe.py` にビルド後検証 (`verify_release`) を追加。実際に EXE を起動して `GET /` を取得し、UI 正本との一致・`<script>` タグの開閉整合・`<body>` の存在・CORS ヘッダ重複の有無を検査し、失敗時はビルドを異常終了させる。

### 📋 再生キューの表示ソート・絞り込み ＆ 一括アップロードの修正 (Queue View & Bulk Upload)
- **画像の一括アップロードで1枚しか登録されなかった問題を修正**:
  - `/api/upload` に「2.5秒に1回」の最小間隔レートリミットが掛かっており、ブラウザが複数枚を連続POSTすると2枚目以降がすべて `429` になっていた（ループバックからの操作では発生せず、LAN・トンネル経由でのみ発生）。
  - アップロードは「複数枚まとめて選ぶ」のが通常の使い方のため、時間枠内の合計枚数方式（既定: 60秒あたり20枚）に変更。連投防止を維持したまま一括アップロードが通るようにした。
  - UI 側も失敗を握り潰さず、`5/8枚を追加しました（失敗: ...）` のように成功数と原因を表示するようにした。
- **再生キューに表示専用の絞り込み・並べ替えを追加**:
  - 種別での絞り込み（すべて / 動画のみ / 画像のみ）と、並べ替え（再生順 / タイトル昇順・降順 / 長さ昇順・降順 / 種別順）を追加。
  - **表示だけを変更し、サーバー上の再生順は一切変更しない**。件数バッジは絞り込み時に `表示件数 / 全件数` 表示に切り替わる。
  - 移動・削除APIは実キューの添字で動くため、表示順を変えても添字がずれないよう実添字を保持して描画する。
  - 既定表示（すべて / 再生順）以外のときは、画面上の隣と実キューの隣が一致せず意図しない位置へ動いてしまうため、ドラッグと上下移動を無効化する（削除は表示順に依存しないため有効のまま）。
- **HLSプレビューの再接続を修正（一時ポート枯渇の防止 ＋ 配信開始前に開いても復帰するように）**:
  - 致命的エラー時に 3 秒ごと `loadSource()` を無条件で呼び直しており、前のローダーを止めないまま読み込みが積み上がってセグメント取得の再試行が際限なく増え、Windows で `net::ERR_ADDRESS_IN_USE` が多発していた。
  - 再接続時に `stopLoad()` を挟んでから `loadSource()` をやり直すようにし、あわせて hls.js 内部の再試行回数にも上限を設定（マニフェスト2回・レベル2回・セグメント3回）。
  - 再試行間隔はバックオフ（2秒×回数、最大10秒）とし、成功時（`MANIFEST_PARSED`）にカウンタと表示をリセットする。
  - **再試行の打ち切りは行わない**。サーバーは起動直後の十数秒や曲の切り替わりで `/stream.m3u8` が一時的に 404 になるため、回数上限で `destroy()` すると、その後配信が始まってもプレビューが二度と復帰しない（実測で確認: 配信復旧後も `readyState: 0` / `paused: true` のまま停止）。
  - なお `startLoad()` は「マニフェストを一度も取得できていない状態」では何も再取得しないため、この経路では `loadSource()` でのやり直しが必須。
- **プレビューを開き直すと復帰まで数十秒かかり「再接続中...」が消えなかった問題を修正**:
  - 破棄済みインスタンス宛ての再接続タイマーが後から発火し、新しいインスタンスへ `stopLoad()` / `loadSource()` を撃ち込んでいた。タイマーを保持して作り直し時に解除し、イベントハンドラとタイマー内でインスタンスの同一性を確認するようにした。
  - プレビューを閉じている間も致命的エラーの再接続が走り続けており、再試行カウンタだけが積み上がっていた（実測: 開き直した時点で既に7回、最大10秒のバックオフを引いて復帰が遅れる）。閉じている間は再接続もセグメント取得も行わないようにした。
  - 開き直しは作り直さず `startLoad(-1)` で再開する。閉じている間にライブウィンドウから消えたセグメントを追いかけて、同一 URL へ 404 を連打する状態にならない。
  - 復帰の判定を `MANIFEST_PARSED` だけに頼っていたため、`startLoad(-1)` での再開ではマニフェストが再解析されず、映像が戻っていても「再接続中... (16)」の表示が残り続けていた（実測: 104秒経っても消えない）。実際にセグメントが積まれた時点（`FRAG_BUFFERED`）を成功と見なしてリセットする。
  - 開き直しても再生が戻らない場合に備え、6秒の監視タイマーで作り直しへ切り替えるフォールバックを追加。
  - 実測（ソース起動・ローカル）: 開き直しからの復帰 **約40秒 → 0.1秒**、再試行カウンタ **7→16 → 0のまま**、閉じている間のセグメント取得 **0件**。起動直後の404窓でページを開いた場合も、再試行3回で復帰し表示は「ライブ再生中」へ戻る。

### 🐛 再生モード・スライドショーの不具合修正 (Playback Mode & Slideshow Fixes)
- **「通常動画」を選んでも音声のみのラジオ再生になっていた問題を修正**:
  - `addVideoUrl()` が追加モード radio のときだけ `set_radio_mode: true` を送り、video のときは何も送っていなかった。`config.json` の `radio_mode` 既定が `true` のため初回から通常動画モードが機能せず、一度ラジオを使うと以降ずっとラジオ固定になっていた。
  - モードのトグルはクリック時点で即座に `set_radio_mode` を送信するよう変更（`selectAddMode()`）。トグル表示は `/api/status` の `radio_mode` に追従させるが、利用者の操作直後3秒間は上書きしない。
    - 追従だけを入れると、2秒間隔のステータスポーリングが選択を巻き戻し、「選んでから数秒後に追加ボタンを押すと元のモードで再生される」という形で不具合が再発するため（実測で確認）。
- **ラジオ背景の切り替え（スライドショー等）が一切効かなかった問題を修正**:
  - UI が `action: "set_radio_bg"` / `background` を送っていたのに対し、サーバーの実装は `action: "set_radio_bg_source"` / `source` であり、Unknown action として無視されていた。
  - 背景セレクトの状態同期も、存在しない `data.radio_background` を参照しており常に既定値を表示していた。正しい `data.radio_bg_source` を参照するよう修正。
- **アップロードした写真が起動・終了のたびに消えていた問題を修正**:
  - `atexit` ハンドラ `cleanup_hls_dir_completely()` と `clean_hls_dir(all_files=True)` が `hls_output/` を再帰削除しており、利用者の写真とラジオカードキャッシュ (`images/`) まで毎回消していた。配信の一時ファイルのみを削除し `images/` は保持するよう変更。
  - これにより、スライドショーを選んでも背景が空で待機画面にフォールバックする問題が解消。
- **スライドショーで一部の写真が一度も表示されない問題を修正**:
  - `concat` + `-stream_loop -1` は常にマニフェストの先頭から再生されるため、「写真枚数 x 表示秒数」が曲の長さを超えると後半の写真が表示されないまま次の曲でまた1枚目に戻り、特定の写真が永久にスキップされていた（例: 写真8枚 x 5秒 = 40秒に対し19秒の曲では5枚目以降が一度も表示されない）。
  - 曲ごとに開始位置をずらし、消化した枚数だけ再生カーソルを進めることで全写真を巡回させるよう修正。
- **写真が無言でスキップされた場合のログを追加**:
  - 静止画送信 FFmpeg は `stderr` を破棄しているため、失敗すると原因不明のまま次の写真へ進んでいた。表示時間を待たずに終了した場合に終了コード付きの警告を出力するようにした。

## [2.5.0] - 2026-08-24

### 🔄 曲切り替わり時のフリーズ・タイムライン停止の解消 ＆ シームレス遷移 (Seamless Track Transitions & Prefetch)
- **タイムスタンプ累積管理 (Cumulative Timestamping / `-output_ts_offset`)**:
  - 曲が切り替わっても送信側FFmpegのPTSが `0秒` にリセットされず、前の曲の再生秒数を累積加算して引き継ぐように修正。
  - 受信側常駐HLS FFmpegがタイムスタンプ不連続（`DTS out of order`）によってパケットを破棄・停止する問題を根本解決。
  - VRChatおよびWebプレイヤー側で、曲が変わってもタイムラインが途切れず滑らかに再生が継続されるよう改善。
- **次曲バックグラウンド先読み (Prefetch & Instant Switch)**:
  - 1曲目の再生中に、バックグラウンドスレッドでキューの先頭にある「次の曲」の `yt-dlp` URL取得およびサムネイル画像合成を事前実行・キャッシュ化（`prefetch_item`）。
  - 曲の終了時にURL解析待ち時間（3〜8秒）がゼロになり、即座に次の曲のストリームを開始。
- **切替待機時間の最適化**:
  - デフォルトの切替待機時間（`video_transition_wait_seconds`）を `5秒` から `1秒` に短縮し、曲間の無駄な無通信・無音空白を撤廃。
- **HLSストリーム受信側の堅牢化**:
  - `ensure_hls_receiver` に `-fflags +nobuffer+flush_packets+genpts+igndts` および `-hls_flags delete_segments+split_by_time+append_list` を追加。

## [2.4.0] - 2026-08-24

### ⚡ 先行バースト生成 ＆ 先読みバッファリング制御（超低遅延・高安定化）
- **高速バースト生成 (Fast Startup Burst)**:
  - 送信側 FFmpeg の等倍速制限（`-re`）を撤廃。
  - 動画・ラジオの再生開始時やスキップ時に、最初の約15秒分（4〜5セグメント）を一瞬（0.5秒以内）で超高速生成。
  - VRChat 内プレイヤーが待ち時間なく即座に再生を開始できるようレスポンスを大幅向上。
- **先行ペーシング制御 (Ahead Pacing & Buffer Retention)**:
  - Python パイプ中継層（`relay_stream_data`）に MPEG-TS PTS（タイムスタンプ）パーサーと時間追従レート制御を実装。
  - 再生中、実時間に対して常に「+15秒先行したバッファマージン」を維持して配信。
  - YouTube CDN（googlevideo）側の通信速度低下や一時的なパケット詰まり、スロットリングが発生しても、15秒のバッファマージンにより VRChat 側の再生が途切れずカクつき・バッファ停止を完全防止。
- **HLS プレイリスト容量の拡張**:
  - `hls_list_size` のデフォルトを `10`（30秒）から `15`（45秒）に拡張し、深い先読み時もプレイヤー側の安全な再生域を確保。
- **再生完了・スキップの整合性**:
  - 送信側が先行完了（EOF）した後も、実時間の経過（動画尺）まで待機してから次の曲へスムーズに遷移。スキップ操作時は即座に次曲へバースト開始。

### 🖼️ ラジオモード時の写真スライドショー自動送り機能の完全対応
- **スライドショー背景のシームレス自動送り**:
  - ラジオモードの背景ソースを「スライドショー (`slideshow`)」に設定し、自動送り（`image_auto_advance: true`）が有効な場合、設定された表示時間（`image_display_duration`、デフォルト15秒など）ごとに写真が自動的に切り替わるように対応。
  - FFmpeg の `concat demuxer`（`-stream_loop -1`）を活用し、YouTube音声の再生を一切途切らせることなく、全写真をループでスムーズに切り替え。
  - 写真キュー内およびキャッシュ内の写真から自動収集・重複排除し、シャッフル再生設定にも連動。
  - QRコードオーバーレイ合成（`overlay_qr_mode`）も各スライドショー画像に適用。

## [2.3.0] - 2026-08-24

### 📻 YouTubeサムネイル＆ラジオ番組風カード画面の自動生成 (Radio Card Visualizer)
- **自動カード画面生成**:
  - YouTube動画再生時、高画質サムネイル（`thumbnail`）とタイトル・アーティスト/チャンネル名を自動取得。
  - サムネイルの拡大ガウスぼかし背景＋角丸アルバムアート＋洗練されたタイポグラフィ（日本語フォント対応）で構成された 1920x1080 静止画カードを Pillow (PIL) で自動合成。
- **超低負荷＆実用性重視のシンプル設計**:
  - 配信形式（HLS）特有の再生位置ズレや無駄な負荷を避けるため、波形イコライザー・再生時間バー・常時QRコード表示・余計なバッジ文字を省いた洗練されたデザインを採用。
  - 生成された静止画カードを FFmpeg（`libx264` 200kbps, 2fps ＋ AAC 128kbps）でエンコードし、超低帯域（約300kbps）のまま高画質・高音質なBGM配信を実現。
- **高速ローカルキャッシュ**:
  - 合成済みカードを `hls_output/images/radio_cache/` に動画IDごとに保存し、次回以降の再生時は即座にロード。
- **GUI / Web API 対応**:
  - `radio_bg_source` のデフォルトを `"card"`（カード画面）に設定。
  - GUI設定画面のラジオ背景選択肢に「Card (サムネイル＆楽曲情報)」を追加し、従来の「Standby (待機画面・QR)」「Slideshow (写真スライドショー)」とシームレスに切り替え可能。

## [2.2.0] - 2026-08-24

### 📻 BGM / ラジオモード (Radio & BGM Mode)
- **YouTube音声ストリーム ＋ 静止画/写真スライドショー合成配信**:
  - YouTube動画から高音質オーディオストリーム（`bestaudio`）のみを抽出し、静止画（待機画面/QRコードカード）またはアップロードされた写真スライドショーと合成してHLS配信。
  - **通信帯域・バッファの極小化**:
    - 通常のYouTube動画ストリーミング（2.5〜5Mbps）に対し、映像を低fps・低ビットレート（`libx264` 200kbps, 2fps）＋AAC音声（128kbps）に抑えることで、合計帯域を約 **250〜350kbps（90%以上削減）** に極小化。
    - VRChat内の大人数インスタンスや回線の細い環境でもバッファ詰まりや読み込み遅延のない安定したBGM・音楽鑑賞を実現。
  - **柔軟な背景ソース切り替え**:
    - `standby` (デフォルト): 待機画面（QRコード & Web Request URLカード）を背景として表示。
    - `slideshow`: ユーザーがアップロードした写真やキュー内の写真から背景画像を選択。
  - **GUI / Webリモコン / REST API 完全統合**:
    - GUIヘッダーおよびSettings画面に「📻 Radio / BGM」トグルスイッチと設定を追加。
    - Webリモコンに「📻 BGM/ラジオ」トグルボタンを追加。
    - `/api/control` に `set_radio_mode`, `set_radio_bg_source` アクションを追加。
    - `/api/status` レスポンスに `radio_mode`, `radio_bg_source` を追加。
    - 再生中のモード切り替え時も位置（シーク）を維持したシームレスなホットリロードに対応。
  - *(※将来の拡張案として記録: YouTubeサムネイル画像を自動取得・加工した「ラジオ風カード」の自動生成)*

## [2.1.0] - 2026-08-24

- **写真・スクリーンショット共有 & スライドショー機能**:
  - **複数写真の一括アップロード**: Webリモコンから複数枚の画像（JPEG, PNG, WebP等）をまとめて選択・キューへ一括追加可能に。
  - **リアルタイム・スライドショー操作 (GUI / Web)**:
    - 写真再生中、GUI下部コントロールバーに「⏸ 一時停止 / ▶ 再開」トグルボタンおよび「⏱ 表示秒数切り替え (5s / 10s / 15s / 30s / 60s / 120s)」コンボボックスを追加。
    - Webリモコンからもスライドショーの一時停止・再開・秒数変更がリアルタイムに操作可能。
- **UI・操作性の整理**:
  - **不要な「Prev（前へ）」ボタンの廃止**: リアルタイムHLSストリーミングおよびキュー保持ループ設計（再生終了曲はキュー末尾へ自動循環）に合わせ、GUIおよびWebリモコンから「前へ / 戻る」ボタンを整理・撤廃。
- **ストリーム映像へのQRコード・URL上書き表示（QR Code Overlay）**:
  - **動画・写真共通の統合設定**:
    - 設定スイッチを1本化（`overlay_qr_enabled`）し、1クリックで動画・写真・待機画面のQRオーバーレイを一括制御可能に。
  - **フル画面オーバーレイの見切れ自動解消**:
    - YouTube動画の解像度（720p, 480p, 1080p等）に合わせてオーバーレイ画像を自動スケーリングし、画面端の見切れを根本防止。
  - **待機画面共通の構造 & URL完全表記（手入力対応）**:
    - QRコードに加え、手入力アクセスが可能なよう完全なWeb Request URL（Full URL）を一切省略せずに明瞭表記。
  - **表示レイアウトモード（コンパクト / フル画面）**:
    - **右下コンパクトモード (Compact / デフォルト)**: 映像右下に 260x260px の角丸白カード（QRコード＋完全URL）を表示。
    - **フル画面モード (Fullscreen)**: 待機画面と同じ大画面レイアウト（中央大QRコード＋タイトル＋完全URL）で映像上にオーバーレイ表示。
  - **シームレス・ホットリロード**:
    - 動画や写真の再生中であっても、設定保存時に再生位置（経過秒数）を自動維持したまま即座に画面上の表示が切り替わるよう改善。
  - **動画再エンコード負荷への注意喚起**:
    - 動画再生時のQRオーバーレイ有効化時は、FFmpegによるリアルタイム再エンコード処理が行われるためバッファが発生しやすくなる旨をUI・ドキュメントに明記。

## [2.0.1] - 2026-08-24

### 🔄 ロールバック & プロセスライフサイクル安定化 (Rollback & Process Lifecycle Fix)
- **安定版コードベースへのロールバック**:
  - 実験的機能（写真フレームフィーダー・QRオーバーレイ）を一時巻き戻し、検証済みの安定版（v2.0.0 ベース）に復元。
- **子プロセス終了処理の完全強化**:
  - アプリ終了時に `cloudflared.exe`、`ffmpeg.exe` および標準入力パイプを確実に即時強制終了（`kill_proc`）するよう修正。
  - バックグラウンドでのゾンビプロセスの残存と、それに起因する一時フォルダロックや Cloudflare Quick Tunnel の多重接続（HTTP 429 / Code 1015）を根本防止。

## [2.0.0] - 2026-08-22

### 🚀 新機能 (New Features)

#### 1. ヘッドレス動作モード & HTTP API サーバー対応
- `--headless` / `-hl` 引数を指定して、GUIを表示せずにバックグラウンドのAPIサーバーとして起動可能になりました。
- 外部アプリケーション（Vue 3フロントエンド等）からHTTP JSON API経由でストリーマーの全機能を遠隔操作可能になりました。
- 全エンドポイントで **CORS (`Access-Control-Allow-Origin: *`)** に完全対応。
- **実装されたAPIエンドポイント**:
  - `GET /api/status`: ストリーマー状態、トンネルURL、再生中動画、キュー一覧、ループ/シャッフル状態を取得
  - `POST /api/queue`: 動画またはプレイリストURLの追加
  - `POST /api/control`: 再生操作（`skip`, `clear_queue`, `stop`, `delete_item`, `move_item`, `shuffle`, `set_loop`, `set_shuffle`）
  - `POST /api/shutdown`: サーバーおよびバックグラウンドプロセスの正常終了
  - `GET /api/config` / `POST /api/config`: 設定の取得および動的更新
  - `GET /` / `GET /stream.m3u8` (ブラウザアクセス時): HLS Web Player HTML配信

#### 2. 設定機能 & 設定画面 (Settings UI & config.json)
- GUI右上に「⚙ Settings」ボタンを追加し、専用の設定モーダルダイアログから以下の項目を変更・永続保存できるようになりました：
  - **サーバーポート (Server Port)**: APIおよびHLS配信ポート（デフォルト: `8000`）
  - **HLSセグメント秒数 / バッファ秒数 (HLS Segment Duration)**: ffmpeg `-hls_time`（デフォルト: `3` 秒）
  - **動画切り替え待機秒数 (Transition Wait Duration)**: 動画終了後のバッファ再生完了待機時間（デフォルト: `5` 秒）
  - **Web Player Live Sync Count**: Web Playerの初期同期バッファ数（デフォルト: `4`）
  - **Loop Queue / Shuffle Play のデフォルトON/OFF設定**
- 設定画面内に**折りたたみ式の「API 仕様・引数リファレンス」**を内蔵。

#### 3. シャッフル再生 & キュー保持ループ再生 (Shuffle & Repeat Playback)
- **即時シャッフル (Shuffle List / Now)**:
  - 「🔀 Shuffle List」ボタンまたは `POST /api/control` (`{"action": "shuffle"}`) で現在のキューを一瞬でランダムに並び替え。
- **シャッフル再生モード (Shuffle Mode)**:
  - 「🔀 Shuffle」スイッチまたは `POST /api/control` (`{"action": "set_shuffle", "enabled": true}`) で、キューから次に再生する曲をランダムに選出。
- **キュー保持ループ再生モード (Loop / Repeat Queue)**:
  - 「🔁 Loop」スイッチまたは `POST /api/control` (`{"action": "set_loop", "enabled": true}`) で有効化。
  - 再生終了（またはスキップ）した動画をキューの末尾へ自動的に再登録し、キューを枯渇させずにエンドレスループ再生。
  - シャッフルとループの併用で「登録動画群のランダムエンドレス再生」が可能。

#### 4. キューのドラッグ＆ドロップ並び替え & オートスクロール (DnD Reordering & Auto-scroll)
- **ドラッグ＆ドロップ並び替え**:
  - 各行のグリップアイコン「`☰`」または動画タイトルをドラッグして直感的に順序を変更可能。
  - ドラッグ中に対象行および移動先プレビューがハイライト表示。
  - 従来の「`▲`」「`▼`」ボタンも維持し、両立して操作可能。
- **自動スクロール (Auto-scroll)**:
  - ドラッグ中にカーソルをキューリストの上端付近または下端付近に持っていくと、リストが自動的に上下へ滑らかにスクロール。

---

### 🛠 内部設計・リファクタリング (Architecture & Refactoring)
- **モジュール分離**:
  - `streamer_core.py`: ストリーミングエンジン、キュー管理、プロセス制御、設定永続化
  - `api_server.py`: Python標準 `http.server` ベースのマルチスレッドCORS APIサーバー
  - `gui_streamer.py`: CLI引数解析、ヘッドレスモード制御、CustomTkinter GUI
- **外部依存の極小化**:
  - APIサーバーはPython標準ライブラリ（`http.server`, `socketserver`, `json`）のみで構築し、PyInstallerでの単一EXEビルド安定性と軽量性を維持。
