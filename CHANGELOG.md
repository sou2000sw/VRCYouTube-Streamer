# 更新履歴 (CHANGELOG)

## [2.6.0] - 2026-08-25 (Development / 開発途中)

### 🧩 VRCBeacon プラグイン対応 ＆ VRCX トーン＆マナー WebUI (Drop-in Plugin & Modern UI)
- **VRCBeacon 向けドロップインプラグイン パッケージ化**:
  - `plugin/` フォルダ配下に `plugin.json`、`icon.svg`、`ui/index.html`、`bin/` を整備。
  - `build_exe.py` に `--plugin-only` オプションを追加し、`releases/vrcbeacon-plugin-vrcyoutube-v{version}.zip` の自動パッケージングに対応。
- **VRCBeacon (VRCX) デザイン完全準拠のプラグイン WebUI (`plugin/ui/index.html`)**:
  - Tailwind CSS + Remix Icon による洗練されたダークテーマ UI。
  - **サーバー起動 & ライフサイクル管理**: サーバー停止時の起動ボタン（IPC / ローカル対応）、稼働時の停止・再起動・WebPlayerボタン。
  - **プレイヤー & キュー管理**: 現在再生中カード、YouTube動画/プレイリスト追加（通常 / 📻 BGMラジオ切替）、写真一括アップロード、ドラッグ＆ドロップ並び替え、シャッフル、ループ切替。
  - **配信・QR設定**: 画面上QRコードオーバーレイ（有効/無効、右下コンパクト/中央全画面）、スライドショー表示秒数・自動送り設定。
  - **ストリーム情報**: VRChat動画プレイヤー用URLのワンクリックコピー。

## [2.5.0] - 2026-08-24

### 🔄 曲切り替わり時のフリーズ・タイムライン停止の解消 ＆ シームレス遷移 (Seamless Track Transitions & Prefetch)
- **タイムスタンプ累積管理 (Cumulative Timestamping / `-output_ts_offset`)**:
  - 曲が切り替わっても送信側FFmpegのPTSが `0秒` にリセットされず、前の曲の再生秒数を累積加算して引き継ぐように修正。
  - 受信側常駐HLS FFmpegがタイムスタンプ不連続（`DTS out of order`）によってパケットを破棄・停止する問題を根本解決。
  - VRChatおよびWebプレイヤー側で、曲が変わってもタイムラインが途切れず滑らかに再生が継続されるよう改善。
- **次曲バックグラウンド先読み (Prefetch & Instant Switch)**:
  - 1曲目の再生中に、バックグラウンドスレッドでキューの先頭にある「次の曲」の `yt-dlp` URL取得およびサムネイル画像合成を事前実行・キャッシュ化（`prefetch_item`）。
  - 曲の終了時にURL解析待ち時間（3〜8秒）がゼロになり、即座に次の曲のストリームを開始。
- **切替待機時間の最適化**:
  - デフォルトの切替待機時間（`video_transition_wait_seconds`）を `5秒` から `1秒` に短縮し、曲間の無駄な無通信・無音空白を撤廃。
- **HLSストリーム受信側の堅牢化**:
  - `ensure_hls_receiver` に `-fflags +nobuffer+flush_packets+genpts+igndts` および `-hls_flags delete_segments+split_by_time+append_list` を追加。

## [2.4.0] - 2026-08-24

### ⚡ 先行バースト生成 ＆ 先読みバッファリング制御（超低遅延・高安定化）
- **高速バースト生成 (Fast Startup Burst)**:
  - 送信側 FFmpeg の等倍速制限（`-re`）を撤廃。
  - 動画・ラジオの再生開始時やスキップ時に、最初の約15秒分（4〜5セグメント）を一瞬（0.5秒以内）で超高速生成。
  - VRChat 内プレイヤーが待ち時間なく即座に再生を開始できるようレスポンスを大幅向上。
- **先行ペーシング制御 (Ahead Pacing & Buffer Retention)**:
  - Python パイプ中継層（`relay_stream_data`）に MPEG-TS PTS（タイムスタンプ）パーサーと時間追従レート制御を実装。
  - 再生中、実時間に対して常に「+15秒先行したバッファマージン」を維持して配信。
  - YouTube CDN（googlevideo）側の通信速度低下や一時的なパケット詰まり、スロットリングが発生しても、15秒のバッファマージンにより VRChat 側の再生が途切れずカクつき・バッファ停止を完全防止。
- **HLS プレイリスト容量の拡張**:
  - `hls_list_size` のデフォルトを `10`（30秒）から `15`（45秒）に拡張し、深い先読み時もプレイヤー側の安全な再生域を確保。
- **再生完了・スキップの整合性**:
  - 送信側が先行完了（EOF）した後も、実時間の経過（動画尺）まで待機してから次の曲へスムーズに遷移。スキップ操作時は即座に次曲へバースト開始。

### 🖼️ ラジオモード時の写真スライドショー自動送り機能の完全対応
- **スライドショー背景のシームレス自動送り**:
  - ラジオモードの背景ソースを「スライドショー (`slideshow`)」に設定し、自動送り（`image_auto_advance: true`）が有効な場合、設定された表示時間（`image_display_duration`、デフォルト15秒など）ごとに写真が自動的に切り替わるように対応。
  - FFmpeg の `concat demuxer`（`-stream_loop -1`）を活用し、YouTube音声の再生を一切途切らせることなく、全写真をループでスムーズに切り替え。
  - 写真キュー内およびキャッシュ内の写真から自動収集・重複排除し、シャッフル再生設定にも連動。
  - QRコードオーバーレイ合成（`overlay_qr_mode`）も各スライドショー画像に適用。

## [2.3.0] - 2026-08-24

### 📻 YouTubeサムネイル＆ラジオ番組風カード画面の自動生成 (Radio Card Visualizer)
- **自動カード画面生成**:
  - YouTube動画再生時、高画質サムネイル（`thumbnail`）とタイトル・アーティスト/チャンネル名を自動取得。
  - サムネイルの拡大ガウスぼかし背景＋角丸アルバムアート＋洗練されたタイポグラフィ（日本語フォント対応）で構成された 1920x1080 静止画カードを Pillow (PIL) で自動合成。
- **超低負荷＆実用性重視のシンプル設計**:
  - 配信形式（HLS）特有の再生位置ズレや無駄な負荷を避けるため、波形イコライザー・再生時間バー・常時QRコード表示・余計なバッジ文字を省いた洗練されたデザインを採用。
  - 生成された静止画カードを FFmpeg（`libx264` 200kbps, 2fps ＋ AAC 128kbps）でエンコードし、超低帯域（約300kbps）のまま高画質・高音質なBGM配信を実現。
- **高速ローカルキャッシュ**:
  - 合成済みカードを `hls_output/images/radio_cache/` に動画IDごとに保存し、次回以降の再生時は即座にロード。
- **GUI / Web API 対応**:
  - `radio_bg_source` のデフォルトを `"card"`（カード画面）に設定。
  - GUI設定画面のラジオ背景選択肢に「Card (サムネイル＆楽曲情報)」を追加し、従来の「Standby (待機画面・QR)」「Slideshow (写真スライドショー)」とシームレスに切り替え可能。

## [2.2.0] - 2026-08-24

### 📻 BGM / ラジオモード (Radio & BGM Mode)
- **YouTube音声ストリーム ＋ 静止画/写真スライドショー合成配信**:
  - YouTube動画から高音質オーディオストリーム（`bestaudio`）のみを抽出し、静止画（待機画面/QRコードカード）またはアップロードされた写真スライドショーと合成してHLS配信。
  - **通信帯域・バッファの極小化**:
    - 通常のYouTube動画ストリーミング（2.5〜5Mbps）に対し、映像を低fps・低ビットレート（`libx264` 200kbps, 2fps）＋AAC音声（128kbps）に抑えることで、合計帯域を約 **250〜350kbps（90%以上削減）** に極小化。
    - VRChat内の大人数インスタンスや回線の細い環境でもバッファ詰まりや読み込み遅延のない安定したBGM・音楽鑑賞を実現。
  - **柔軟な背景ソース切り替え**:
    - `standby` (デフォルト): 待機画面（QRコード & Web Request URLカード）を背景として表示。
    - `slideshow`: ユーザーがアップロードした写真やキュー内の写真から背景画像を選択。
  - **GUI / Webリモコン / REST API 完全統合**:
    - GUIヘッダーおよびSettings画面に「📻 Radio / BGM」トグルスイッチと設定を追加。
    - Webリモコンに「📻 BGM/ラジオ」トグルボタンを追加。
    - `/api/control` に `set_radio_mode`, `set_radio_bg_source` アクションを追加。
    - `/api/status` レスポンスに `radio_mode`, `radio_bg_source` を追加。
    - 再生中のモード切り替え時も位置（シーク）を維持したシームレスなホットリロードに対応。
  - *(※将来の拡張案として記録: YouTubeサムネイル画像を自動取得・加工した「ラジオ風カード」の自動生成)*

## [2.1.0] - 2026-08-24

- **写真・スクリーンショット共有 & スライドショー機能**:
  - **複数写真の一括アップロード**: Webリモコンから複数枚の画像（JPEG, PNG, WebP等）をまとめて選択・キューへ一括追加可能に。
  - **リアルタイム・スライドショー操作 (GUI / Web)**:
    - 写真再生中、GUI下部コントロールバーに「⏸ 一時停止 / ▶ 再開」トグルボタンおよび「⏱ 表示秒数切り替え (5s / 10s / 15s / 30s / 60s / 120s)」コンボボックスを追加。
    - Webリモコンからもスライドショーの一時停止・再開・秒数変更がリアルタイムに操作可能。
- **UI・操作性の整理**:
  - **不要な「Prev（前へ）」ボタンの廃止**: リアルタイムHLSストリーミングおよびキュー保持ループ設計（再生終了曲はキュー末尾へ自動循環）に合わせ、GUIおよびWebリモコンから「前へ / 戻る」ボタンを整理・撤廃。
- **ストリーム映像へのQRコード・URL上書き表示（QR Code Overlay）**:
  - **動画・写真共通の統合設定**:
    - 設定スイッチを1本化（`overlay_qr_enabled`）し、1クリックで動画・写真・待機画面のQRオーバーレイを一括制御可能に。
  - **フル画面オーバーレイの見切れ自動解消**:
    - YouTube動画の解像度（720p, 480p, 1080p等）に合わせてオーバーレイ画像を自動スケーリングし、画面端の見切れを根本防止。
  - **待機画面共通の構造 & URL完全表記（手入力対応）**:
    - QRコードに加え、手入力アクセスが可能なよう完全なWeb Request URL（Full URL）を一切省略せずに明瞭表記。
  - **表示レイアウトモード（コンパクト / フル画面）**:
    - **右下コンパクトモード (Compact / デフォルト)**: 映像右下に 260x260px の角丸白カード（QRコード＋完全URL）を表示。
    - **フル画面モード (Fullscreen)**: 待機画面と同じ大画面レイアウト（中央大QRコード＋タイトル＋完全URL）で映像上にオーバーレイ表示。
  - **シームレス・ホットリロード**:
    - 動画や写真の再生中であっても、設定保存時に再生位置（経過秒数）を自動維持したまま即座に画面上の表示が切り替わるよう改善。
  - **動画再エンコード負荷への注意喚起**:
    - 動画再生時のQRオーバーレイ有効化時は、FFmpegによるリアルタイム再エンコード処理が行われるためバッファが発生しやすくなる旨をUI・ドキュメントに明記。

## [2.0.1] - 2026-08-24

### 🔄 ロールバック & プロセスライフサイクル安定化 (Rollback & Process Lifecycle Fix)
- **安定版コードベースへのロールバック**:
  - 実験的機能（写真フレームフィーダー・QRオーバーレイ）を一時巻き戻し、検証済みの安定版（v2.0.0 ベース）に復元。
- **子プロセス終了処理の完全強化**:
  - アプリ終了時に `cloudflared.exe`、`ffmpeg.exe` および標準入力パイプを確実に即時強制終了（`kill_proc`）するよう修正。
  - バックグラウンドでのゾンビプロセスの残存と、それに起因する一時フォルダロックや Cloudflare Quick Tunnel の多重接続（HTTP 429 / Code 1015）を根本防止。

## [2.0.0] - 2026-08-22

### 🚀 新機能 (New Features)

#### 1. ヘッドレス動作モード & HTTP API サーバー対応
- `--headless` / `-hl` 引数を指定して、GUIを表示せずにバックグラウンドのAPIサーバーとして起動可能になりました。
- 外部アプリケーション（VRCBeaconやVue 3フロントエンド等）からHTTP JSON API経由でストリーマーの全機能を遠隔操作可能になりました。
- 全エンドポイントで **CORS (`Access-Control-Allow-Origin: *`)** に完全対応。
- **実装されたAPIエンドポイント**:
  - `GET /api/status`: ストリーマー状態、トンネルURL、再生中動画、キュー一覧、ループ/シャッフル状態を取得
  - `POST /api/queue`: 動画またはプレイリストURLの追加
  - `POST /api/control`: 再生操作（`skip`, `clear_queue`, `stop`, `delete_item`, `move_item`, `shuffle`, `set_loop`, `set_shuffle`）
  - `POST /api/shutdown`: サーバーおよびバックグラウンドプロセスの正常終了
  - `GET /api/config` / `POST /api/config`: 設定の取得および動的更新
  - `GET /` / `GET /stream.m3u8` (ブラウザアクセス時): HLS Web Player HTML配信

#### 2. 設定機能 & 設定画面 (Settings UI & config.json)
- GUI右上に「⚙ Settings」ボタンを追加し、専用の設定モーダルダイアログから以下の項目を変更・永続保存できるようになりました：
  - **サーバーポート (Server Port)**: APIおよびHLS配信ポート（デフォルト: `8000`）
  - **HLSセグメント秒数 / バッファ秒数 (HLS Segment Duration)**: ffmpeg `-hls_time`（デフォルト: `3` 秒）
  - **動画切り替え待機秒数 (Transition Wait Duration)**: 動画終了後のバッファ再生完了待機時間（デフォルト: `5` 秒）
  - **Web Player Live Sync Count**: Web Playerの初期同期バッファ数（デフォルト: `4`）
  - **Loop Queue / Shuffle Play のデフォルトON/OFF設定**
- 設定画面内に**折りたたみ式の「API 仕様・引数リファレンス」**を内蔵。

#### 3. シャッフル再生 & キュー保持ループ再生 (Shuffle & Repeat Playback)
- **即時シャッフル (Shuffle List / Now)**:
  - 「🔀 Shuffle List」ボタンまたは `POST /api/control` (`{"action": "shuffle"}`) で現在のキューを一瞬でランダムに並び替え。
- **シャッフル再生モード (Shuffle Mode)**:
  - 「🔀 Shuffle」スイッチまたは `POST /api/control` (`{"action": "set_shuffle", "enabled": true}`) で、キューから次に再生する曲をランダムに選出。
- **キュー保持ループ再生モード (Loop / Repeat Queue)**:
  - 「🔁 Loop」スイッチまたは `POST /api/control` (`{"action": "set_loop", "enabled": true}`) で有効化。
  - 再生終了（またはスキップ）した動画をキューの末尾へ自動的に再登録し、キューを枯渇させずにエンドレスループ再生。
  - シャッフルとループの併用で「登録動画群のランダムエンドレス再生」が可能。

#### 4. キューのドラッグ＆ドロップ並び替え & オートスクロール (DnD Reordering & Auto-scroll)
- **ドラッグ＆ドロップ並び替え**:
  - 各行のグリップアイコン「`☰`」または動画タイトルをドラッグして直感的に順序を変更可能。
  - ドラッグ中に対象行および移動先プレビューがハイライト表示。
  - 従来の「`▲`」「`▼`」ボタンも維持し、両立して操作可能。
- **自動スクロール (Auto-scroll)**:
  - ドラッグ中にカーソルをキューリストの上端付近または下端付近に持っていくと、リストが自動的に上下へ滑らかにスクロール。

---

### 🛠 内部設計・リファクタリング (Architecture & Refactoring)
- **モジュール分離**:
  - `streamer_core.py`: ストリーミングエンジン、キュー管理、プロセス制御、設定永続化
  - `api_server.py`: Python標準 `http.server` ベースのマルチスレッドCORS APIサーバー
  - `gui_streamer.py`: CLI引数解析、ヘッドレスモード制御、CustomTkinter GUI
- **外部依存の極小化**:
  - APIサーバーはPython標準ライブラリ（`http.server`, `socketserver`, `json`）のみで構築し、PyInstallerでの単一EXEビルド安定性と軽量性を維持。
